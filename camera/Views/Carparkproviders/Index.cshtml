@model IEnumerable<camera.DAL.carparkprovider>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.ProviderName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.ActiveAccountNum)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.ProviderName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.ActiveAccountNum)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.CarparkProviderID }) |
            @Html.ActionLink("Details", "Details", new { id=item.CarparkProviderID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.CarparkProviderID })
        </td>
    </tr>
}

</table>
